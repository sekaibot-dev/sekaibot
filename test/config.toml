[bot]
# 这里是 KafuBot 自身的配置
nodes = []
node_dirs = ["./test/nodes"]
adapters = ["sekaibot.adapters.cqhttp"]

[bot.log]
# 这里是 KafuBot 自身日志相关的配置
level = "INFO"
verbose_exception = true



[node.a]
a = "httpa"

[node.b]
a = "httpb"